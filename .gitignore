# Project docs (private)
# CLAUDE.md is deliberately NOT ignored (phase 74): it is the always-loaded
# project brief and part of the public workflow. Nothing secret goes in it.
/PLAN.md
TROUBLESHOOTING.md
Troubleshooting.md
/PHASE-*.txt
skills-rules-agents-interview-notes.txt

# Private docs at the docs/ root (named explicitly — a bare docs/*.md would
# silently swallow any new doc dropped there).
docs/PORTFOLIO.md
docs/INTERVIEW_GUIDE.md
docs/DEMO_SCRIPT.md
docs/SCREENSHOT_CHECKLIST.md
docs/CLAUDE-CODE-WORKFLOW.md

# Claude Code workspace. Tracked on purpose since phase 74 — the agents,
# skills, rules and permission policy are part of the project. Only the
# machine-local pieces stay out.
.claude/settings.local.json
.claude/scheduled_tasks.lock

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/

# Django
*.log
local_settings.py
db.sqlite3
db.sqlite3-journal
media/
staticfiles/

# Environment
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build
dist/
build/
*.egg-info/
*.tsbuildinfo

# Pre-refactor source snapshots kept locally only (git history has them)
backups/

# Coverage
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover
.pytest_cache/

# Frontend
frontend/dist/
frontend/.vite/
