THINGS ONLY YOU CAN DO — PHASE 39 (written 2026-07-20 by Claude)
================================================================
All code is done and verified (372 tests green, tsc clean, lint at
baseline, build guard proven). What remains is dashboard work that
needs your accounts. Full detail with exact settings is in:

    docs/runbooks/phase-39-deploy-steps.txt   (steps 1-6)

Short version, in order:

1. MERGE the PR from branch feat/phase-39-frontend-pages-media-r2.
   (Heads-up: after merge, Render picks up USE_R2=true and will fail
   to boot until step 3's secrets are saved — that failure is the
   fail-fast guard working, not a bug. Doing step 2+3 right after the
   merge, or even before it, avoids the red window.)

2. CLOUDFLARE: create R2 bucket "stemquest-media", enable its public
   r2.dev subdomain, create a bucket-scoped API token (Object Read &
   Write). Note 4 things: access key id, secret, your account id, and
   the pub-<hash>.r2.dev host.

3. RENDER (stemquest-api > Environment): add the five R2_* values from
   step 2. Never paste these into chat.

4. CLOUDFLARE WORKERS (Pages git-connect is retired — use the
   "Create a Worker" flow you found): project name "stemquest", path
   "frontend", build "npm run build", deploy "npx wrangler deploy",
   uncheck non-production-branch builds, and build variables
   NODE_VERSION=22 and
   VITE_API_URL=https://stemquest-api.onrender.com/api — the /api
   suffix is required. Merge PR #26 BEFORE deploying (the build needs
   frontend/wrangler.jsonc from that PR). Note the resulting
   https://stemquest.<subdomain>.workers.dev URL.

5. RENDER again: switch CORS_ALLOWED_ORIGINS, CSRF_TRUSTED_ORIGINS and
   FRONTEND_URL from localhost:5173 to the workers.dev URL. Https scheme, no
   trailing slash.

6. Tell Claude the workers.dev URL — I can then run the curl verification
   (steps in the runbook) with you, and you do the final click-through
   (register, enroll, avatar upload, attachment download, CSV export).

This file is yours — delete it once everything is checked off. It is
not committed to git.
