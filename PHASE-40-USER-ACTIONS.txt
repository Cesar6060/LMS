THINGS ONLY YOU CAN DO — PHASE 40 (written 2026-07-21 by Claude)
================================================================
All code is merged (PR #29) and live. Your earlier dashboard work
went further than the last handoff recorded: the Cloudflare build
vars ARE set — I verified the live bundle carries the React DSN and
Sentry debug IDs, so frontend Sentry is live and source maps were
uploaded. Maps are confirmed NOT publicly served. /verify-stack is
green (376 tests, tsc 0 errors, lint 0 errors / 22 warnings).

What remains is proving events actually arrive in Sentry. Full
detail in docs/runbooks/phase-40-observability-steps.txt steps 6-7.

Short version, in order:

1. BACKEND SMOKE TEST (Render + sentry.io, ~5 min):
   a. Render > stemquest-api > Environment: add
        SENTRY_DEBUG_ENDPOINT = true
      (saving redeploys, ~2 min)
   b. Tell Claude "debug endpoint is on" — I curl it and confirm the
      500. (Or do it yourself:
        curl -s -o /dev/null -w "%{http_code}\n" \
          https://stemquest-api.onrender.com/api/sentry-debug/ )
   c. sentry.io > stemquest-django: a ZeroDivisionError event should
      appear with environment=production and a release SHA. OPEN the
      event and confirm NO user email/IP attached (the PII check).
   d. Render: REMOVE SENTRY_DEBUG_ENDPOINT (redeploys again).
      Tell Claude — I re-curl and confirm the 404 is back.

2. FRONTEND SMOKE TEST (browser + sentry.io, ~3 min):
   a. Open https://stemquest.cesarvillarreal11.workers.dev/ ,
      open devtools console, run:
        setTimeout(() => { throw new Error('phase-40 smoke') })
   b. sentry.io > stemquest-react: the event should show a READABLE
      stack trace with real file names/lines (proves the source-map
      upload worked) and the route name from router instrumentation.

3. CLICK-THROUGH (browser, ~5 min): register/login, course map, open
   a lesson on the live site. Console clean, and sentry.io shows no
   noise events from normal browsing (no 4xx spam).
   Optional while in /admin/: delete Phase 39 leftover account
   r2-check@example.com.

UptimeRobot (old step 8) stays DESCOPED — no cold start exists, so
no keep-warm need; you accepted living without down alerts.
