# Core Django
Django==4.2.30
djangorestframework==3.15.2
django-cors-headers==4.7.0

# Database
psycopg[binary,pool]==3.2.4
dj-database-url==2.3.0

# Production serving
gunicorn==23.0.0
whitenoise[brotli]==6.8.2

# Authentication
django-allauth==65.14.1
dj-rest-auth==7.0.1
djangorestframework-simplejwt==5.5.1
# Transitive via simplejwt, pinned explicitly so it can't drift back below the
# advisory line (7 advisories against < 2.13.0).
pyjwt==2.13.0
requests==2.34.2

# Security headers
django-csp==4.0

# Files & Images
pillow==12.3.0
django-storages[s3]==1.14.6

# Environment variables
python-decouple==3.8

# Error Tracking
sentry-sdk[django]==2.19.2

# Testing
pytest-django==4.9.0
pytest-cov==6.0.0
