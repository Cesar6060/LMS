PHASE 39 DEPLOY RUNBOOK — Frontend on Cloudflare Workers + media on R2
=====================================================================
Written 2026-07-20. Code side is done and verified locally (pytest 372,
tsc clean, lint at baseline, build guard proven). Everything below the
"CODE" section happens in dashboards and is done by Cesar.

Statuses: [x] done  [ ] not yet run. Update this file as steps complete,
in the order they were actually run.


CODE (done, on branch feat/phase-39-frontend-pages-media-r2)
------------------------------------------------------------
[x] django-storages[s3]==1.14.6 added to backend/requirements.txt
[x] USE_R2-gated STORAGES block in backend/config/settings.py
    (inert unless USE_R2=true; missing R2_* var fails the boot on purpose)
[x] render.yaml: USE_R2="true" + five R2_* vars marked sync:false
[x] .env.example documents USE_R2 + R2_* (commented out, off by default)
[x] SPA deep-link fallback: frontend/wrangler.jsonc not_found_handling
    (was _redirects; replaced 2026-07-21 when Pages pivoted to Workers)
[x] vite.config.ts: production build FAILS if VITE_API_URL is unset
[x] CI "Production build" step now passes a dummy VITE_API_URL
[x] Tests: 8 storage-settings + 5 avatar + 5 attachment = 372 total green


STEP 1 — MERGE (Cesar)
----------------------
[x] Open PR from feat/phase-39-frontend-pages-media-r2 into main,
    confirm CI is green, merge.
    Note: merging redeploys the Render API with USE_R2="true" from
    render.yaml. Render will prompt for the five R2_* values; until they
    are saved the deploy will fail at boot BY DESIGN (fail-fast guard).
    Do step 2 first, or accept one red deploy in between.


STEP 2 — CLOUDFLARE R2 (Cesar, Cloudflare dashboard)
----------------------------------------------------
[x] R2 > Create bucket. Suggested name: stemquest-media
[x] Bucket > Settings > Public access > enable "r2.dev subdomain".
    WRITE DOWN the host it gives you, looks like: pub-<hash>.r2.dev
    (host only, no https:// prefix)
[x] R2 > Manage R2 API Tokens > Create API token:
      - Permission: Object Read & Write
      - Scope: ONLY the stemquest-media bucket
    WRITE DOWN: Access Key ID and Secret Access Key (secret shows once)
[x] Also note your Cloudflare Account ID (right sidebar of the R2 page
    or dashboard home).
    No bucket CORS config needed — media is loaded via <img>/<a> tags,
    not fetch/XHR.


STEP 3 — RENDER ENV VARS (Cesar, Render dashboard > stemquest-api >
------------------------------------------------------------------
Environment)
[x] R2_ACCESS_KEY_ID      = <from step 2>
[x] R2_SECRET_ACCESS_KEY  = <from step 2>
[x] R2_ACCOUNT_ID         = <Cloudflare account id>
[x] R2_BUCKET_NAME        = stemquest-media
[x] R2_PUBLIC_HOST        = pub-<hash>.r2.dev        (no scheme!)
    (USE_R2 itself comes from render.yaml after the merge; if the
    dashboard does not show it yet, add USE_R2=true manually.)
    Saving env vars auto-redeploys (~2 min stale window).

DO NOT paste any of these values into chat or commit them anywhere.


STEP 4 — CLOUDFLARE WORKERS BUILDS (Cesar, Cloudflare dashboard)
----------------------------------------------------------------
PIVOT (2026-07-21): the classic Pages git-connect flow is retired for
new projects — the dashboard only offers "Create a Worker". The
frontend now deploys as an assets-only Worker instead. The repo carries
frontend/wrangler.jsonc (added to PR #26) which handles the SPA
deep-link fallback (not_found_handling) — _redirects was removed.

MERGE PR #26 FIRST — the dashboard builds from main, and main needs
wrangler.jsonc + the Vite guard before the first build can succeed.

[x] Workers & Pages > Create > connect Cesar6060/LMS, then:
      Project name:    stemquest   (must match "name" in wrangler.jsonc)
      Path:            frontend
      Build command:   npm run build
      Deploy command:  npx wrangler deploy
      "Builds for non-production branches": UNCHECK (previews would
      fail CORS anyway — accepted scope cut in the spec)
      API token: "Create new token" (auto) is fine
[x] Build variables:
      VITE_API_URL = https://stemquest-api.onrender.com/api
      NODE_VERSION = 22
    THE /api SUFFIX IS REQUIRED — the frontend appends nothing.
    (If you forget VITE_API_URL entirely the build fails on purpose
    with a clear error — that is the new guard working.)
[x] Deploy. WRITE DOWN the URL: https://stemquest.<subdomain>.workers.dev


STEP 5 — POINT THE API AT THE NEW FRONTEND (Cesar, Render dashboard)
--------------------------------------------------------------------
Update these three existing env vars from http://localhost:5173 to the
workers.dev URL from step 4. Formatting traps from Phase 38 apply: exact
scheme (https://), NO trailing slash, comma-separated with no spaces if
multiple values.
[x] CORS_ALLOWED_ORIGINS = https://<worker>.workers.dev
[x] CSRF_TRUSTED_ORIGINS = https://<worker>.workers.dev
[x] FRONTEND_URL         = https://<worker>.workers.dev
    (Again: save triggers a ~2 min redeploy.)


STEP 6 — VERIFY (either of us; curl commands can be run by Claude)
------------------------------------------------------------------
[x] curl -I https://<worker>.workers.dev/            -> 200
[x] curl -I https://<worker>.workers.dev/courses     -> 200 serving
    index.html (proves the SPA fallback / deep-link refresh)
[x] curl -H "Origin: https://<worker>.workers.dev" -I \
      https://stemquest-api.onrender.com/api/health/
    -> response includes access-control-allow-origin
[x] Upload an avatar in the app, then confirm:
      - object appears in the stemquest-media bucket
      - avatar URL is https://pub-<hash>.r2.dev/avatars/... and serves
        200 with an image content-type
[x] Full prod click-through at the workers.dev URL: (user-verified 2026-07-21)
      register new student -> enroll in JAVA101 -> open course map ->
      HARD-REFRESH a deep link -> upload avatar (renders in header +
      settings) -> instructor uploads lesson attachment -> student
      downloads it -> gradebook CSV export works.
      Zero CORS / mixed-content errors in the browser console.
[ ] Opportunistic: note one Render cold-start timing figure
    (first request after ~15 min idle).


AS-RUN NOTES (2026-07-21, deploy night)
---------------------------------------
- Live URL: https://stemquest.cesarvillarreal11.workers.dev
- Pages git-connect flow no longer exists -> Workers Builds pivot
  (PR #27). First two Cloudflare builds failed usefully:
  1) VITE_API_URL missing -> the vite guard error, verbatim. Fix: the
     variable must be a BUILD variable (Settings > Build), not a
     runtime Worker variable.
  2) _redirects rejected by Workers assets as an infinite-loop rule
     (error 100324) -> removed in PR #27; wrangler.jsonc
     not_found_handling replaces it.
- CORS trap hit again as predicted: first paste of the workers.dev
  origin did not match (no a-c-a-o header on ANY origin). Final value
  holds both prod + localhost, comma-separated no spaces.
- R2_ACCOUNT_ID trap: the API token ID hash was pasted instead of the
  account ID -> boto SSLV3_ALERT_HANDSHAKE_FAILURE on
  <wrong-id>.r2.cloudflarestorage.com. The account id is the hash in
  dashboard URLs (c47a5512...), not the token id.
- Avatar upload verified end-to-end via a throwaway API account
  (r2-check@example.com, no privileges; delete in /admin/ if desired):
  200 -> https://pub-28b0ff93....r2.dev/avatars/tiny.png -> served
  200 image/png -> delete -> object 404. Content-Type note: the served
  type mirrors what the uploading client sends (browsers send
  image/png; bare curl -F sends octet-stream).
- Deep health green through all of it: {"status":"ok","database":"ok"}


ROLLBACK
--------
Media: set USE_R2=false in Render env -> back to (ephemeral) disk.
Frontend: Workers keeps prior versions; roll back in the dashboard.
CORS: re-add http://localhost:5173 alongside the workers.dev URL if you need
the local-Vite-against-prod-API workflow again (comma-separated).
