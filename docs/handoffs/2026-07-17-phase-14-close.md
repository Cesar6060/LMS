# Handoff: Phase 14 Permissions — closed out; ready for Analytics phase

## Current state
Phase 14 (Fixing Permissions) is **fully complete and merged**.
- PR #3 was already merged to `lms/main` before this session (ca1c973).
- This session did the last spec item, the manual UI click-through, via headless Chrome (playwright-core against the running stack):
  1. instructor2@demo.com → `/instructor/courses/VGD101/gradebook` → AccessDenied card, URL preserved ✓
  2. student1@demo.com (not enrolled in ROB201) → `/courses/ROB201/discussions` → AccessDenied ✓
  3. student1@demo.com (enrolled in CS101) → `/courses/CS101/discussions` → normal page ✓
  Also verified instructor catalog browse-all: instructor2 (owns 0 courses) sees all 3 via `/api/courses/courses/`.
- `docs/specs/phase-14-fixing-permissions.md`: all items now checked; committed as 577caa9 on `main`, pushed to `lms`.
- Verified before that commit: pytest **206 passed** · `tsc --noEmit` 0 errors · lint 0 errors (28 pre-existing exhaustive-deps warnings).
- Local `main` is synced with `lms/main`. Branch `feat/phase-14-permissions` can be deleted.

## In progress / not done
- Nothing half-finished. Next phase not started (blocked on `/start-phase` interview — user-invoked only).

## Next steps
1. Run `/start-phase` for PLAN.md "Phase 14: Instructor Analytics Dashboard" (~line 1485). Name the spec `phase-15-instructor-analytics.md` (or similar) — the phase-14 name is taken by the permissions spec.
2. Branch off `lms/main` (e.g. `feat/phase-15-analytics`).
3. Backend: 4 analytics endpoints under `/api/courses/{code}/analytics/…` (overview, assignments, students, activity) — use `require_course_instructor` from `courses/permissions.py` from day one. Frontend: AnalyticsDashboard page + recharts.

## Decisions made
- Created `instructor2@demo.com` / `Admin123!` (owns no courses) in the dev DB for cross-instructor testing — kept for future permission testing.
- UI click-through automated with playwright-core driving installed Chrome (`channel: 'chrome'`) from the session scratchpad; nothing installed in the repo. `chromium-cli` is not on this machine.
- Spec checkbox commit went straight to `main` (docs-only) after a full green verify run, per the "verify before main" rule.

## Gotchas discovered
- `/api/courses/` is the DRF router root; the course list is `/api/courses/courses/`.
- `/api/auth/login/` returns `{"key": ...}` (token auth), not JWT `access`/`refresh` — use `Authorization: Token <key>` for API probes.
- Seed data WAS present in the dev DB (instructor@demo.com + student1-5, password `Admin123!`) despite the previous handoff saying seed_data was never run.
- Broken `head` shell alias still present (hijacks even piped `head` in some shells) — use `sed -n`/`tail`.
- `/start-phase` and `/handoff` have `disable-model-invocation` — the user must type them.

## Files to read first
- `PLAN.md` ~line 1485 (Instructor Analytics Dashboard phase)
- `backend/courses/permissions.py` (permission helpers to reuse in analytics endpoints)
- `docs/specs/phase-14-fixing-permissions.md` (conventions the analytics endpoints must follow)
- `frontend/src/components/AccessDenied.tsx` + `frontend/src/services/api.ts` (403 UX pattern for the new page)
